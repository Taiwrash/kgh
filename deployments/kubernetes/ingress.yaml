apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kgh
  namespace: default
  # annotations:
    # kubernetes.io/ingress.class: traefik
spec:
  ingressClassName: traefik
  rules:
  - host: kgh.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kgh
            port:
              number: 80
